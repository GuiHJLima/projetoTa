'use client';
import styles from './page.module.css';
const Header = () => {
    return (
        <header className={styles.headerContainer}>
            <h1>Header</h1>
        </header>
    );
}
export default Header;